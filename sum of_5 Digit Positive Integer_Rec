#include<stdio.h>

int rec(int number);
int main(){
    printf("give me a five digit number");
    scanf("%d",&number);

  int sum=sum(number);
  printf("%d\n",number);
}

int rec(int number){

    int sum =o;
    if(number ==0){
        return number;
    }

    sum=number%10+rec(number\10);
    return(sum);
    
}
